package com.example.geometry;
// question to crate a 2 package and in one geometry package create a class circle and rectangle
// and calculate it's into the package example .util
public class Rectangle {

    public double length;
    public double breadth;

    public Rectangle(double length, double breadth){
        this.length =length;
        this.breadth=breadth;
    }
}
