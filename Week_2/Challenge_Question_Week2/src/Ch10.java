public class Ch10 {
    /*
    crate a book class for a library system and instance variables title, author, isbn,
    static variable : total books, instance method: returnbook(), bowworBooks()
    Static method  : getTotalbooks(), to get the total numbers of the books in the library
     **/

    // problem solved in a book class : thank you......



    /*
    q.2 = design a Course class;
    instance variable : CourseName, EnrolledStudent;
    static variable = maxCapacity , maximum student of any course
    instance method = enrolledStudent(string NameofStudent), unEnrolledStudent(String studentName)
    static method = setMaxCapacity(int capacity)

     */
}
